# مراجعة Bubble — منصة مدارس المشكاة V1.0.15

## قاعدة الأشخاص المعتمدة
- **الطالب لا يُكتب يدويًا في أي نموذج**: أي حقل شخص من نوع طالب يستخدم قائمة من `Students` ويحفظ Bubble ID، والاسم المعروض يؤخذ من `Full Name`.
- **الموظف لا يُكتب يدويًا في أي نموذج**: أي حقل شخص من نوع موظف يستخدم قائمة من `Users Data` ويحفظ Bubble ID، بصرف النظر عن المسمى الوظيفي.
- قوائم الطلاب تُقيد بمدرسة/مجمع/مرحلة المستخدم.
- قوائم الموظفين تُقيد بالمدرسة/المجمع، **ولا تُقيد بالمرحلة أو المسمى الوظيفي** حتى يمكن اختيار أي موظف داخل المدرسة.
- اسم الموجه الحالي، مدير المدرسة، المدرسة/المجمع/المرحلة، العام الأكاديمي والفصل الدراسي بيانات تلقائية وليست حقول كتابة.

## الجداول الجديدة المعتمدة
`Guidance_Plan`, `Guidance_Plan_Item`, `Guidance_Event`, `Guidance_Message`, `Guidance_Presentation`, `Guidance_Certificate`, `Guidance_Template`.

## ملاحظات مطلوبة في Bubble
### 1) Guidance_Log — تعديل مهم
في الصور يوجد حقلان ظاهرين باسم `year`: أحدهما نوع `academic year` والآخر نوع `terms`. يجب إعادة تسمية حقل الفصل الدراسي إلى **`Term`** (أو اسم مختلف واضح) حتى لا يصبح مفتاح الـAPI ملتبسًا.

### 2) Guidance_Mettings — تعديل موصى به
واجهة «المطلوب مقابلته» أصبحت قائمة موظفين من `Users Data`، لكن جدول `Guidance_Mettings` الحالي لا يحتوي علاقة موظف. أضف:
- `MeetingEmployee` → **Users Data**
بعد إضافته يمكن حفظ الموظف كعلاقة فعلية بدل حفظ اسمه داخل `Details` كحل توافق مؤقت.

### 3) School — تحقق من وجود الحقلين التاليين
لضمان أن المدير ونوع المدرسة آليان 100% بدون استنتاج:
- `Manager` → **Users Data**
- `SchoolType` → text أو Option Set بقيم `boys/girls` أو `بنين/بنات`
إذا كان عندك بالفعل حقول تؤدي نفس الغرض، لا تنشئ حقولًا مكررة؛ فقط نحتاج أسماءها الحقيقية لربطها.

## ليس مطلوبًا إنشاء جداول إضافية
الجداول السبعة الجديدة كافية للخطة والتقويم والمراسلات والعروض والشهادات والقوالب. التقارير تُولد من البيانات الموجودة ولا تحتاج Data Type مستقل حاليًا.

## متطلب تكامل وليس جدولًا
لكي تتعرف الواجهة المستقلة على **المستخدم الحالي** تلقائيًا بشكل آمن، يلزم Backend Workflow/Directory Endpoint في Bubble يعيد `currentUsersData` (وسياق المدرسة/التوزيع) للمستخدم المسجل. لا يوضع Admin API Token داخل JavaScript العام.
