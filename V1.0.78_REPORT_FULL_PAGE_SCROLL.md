# V1.0.78 — Full-page report scrolling

- Removed the internal max-height from the collective guidance report table shell.
- The report now expands vertically with all rows; only the browser/page scroll is used.
- Removed the desktop inner report scrollbar by fitting the four report columns to the available width.
- Added a narrow-screen card layout so the report stays readable without nested scrolling.
- No database, API workflow, save/update/delete, or report-data mapping changes.
