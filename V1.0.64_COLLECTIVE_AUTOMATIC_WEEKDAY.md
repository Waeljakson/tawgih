# V1.0.64 — Automatic weekday in collective guidance

The `day` field in the `group_guidance` form is no longer a text input. It is displayed as a computed Arabic weekday derived from the record meta date (`metaDate`). A hidden `day` form value is synchronized for saved form data and reports.
