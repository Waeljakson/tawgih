# V1.0.47 — Job Titels chat fix

Confirmed Bubble schema:
- Data type: `Job Titels`
- Job label field: `Job Titel`
- Users Data relation: `Current Job`

Fix:
1. Loads the exact `Job Titels` Data API type plus technical fallback aliases.
2. Canonicalizes the populated job-title collection as `jobTitles`.
3. Resolves `Users Data -> Current Job` relation ID against Job Titels.
4. Reads `Job Titel` as the human-readable role.
5. Chat filters resolved role for `موجه طلابي`.
6. Chat displays `Users Data -> Full Name`.

No Bubble schema/privacy change is required for this frontend fix.
