# V1.0.40

- Restores the missing `normalizeStudentName()` helper in `records/app.js`.
- Fixes the runtime `ReferenceError` that stopped records initialization after Bubble directory loading.
- Adds console diagnostics: studentClassesReceived and studentsWithClass.
- Keeps all V1.0.35 direct `student_classes` mapping behavior.
