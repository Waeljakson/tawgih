# V1.0.62 — Plan Delete Workflow

- ربط حذف الخطة بـ Bubble Backend Workflow: `guidance_delete_plan`.
- الحذف يتم باستخدام User Bearer Token الحالي، بدون Admin token وبدون إدخال IDs يدويًا من المستخدم.
- إرسال `grade / school / department / academic_year / term` تلقائيًا لتحديد نفس الخطة.
- لا تُزال الخطة من الواجهة إلا بعد أن يرجع Bubble `success = yes/true`.
- إزالة علامة الخطة المحلية بعد نجاح الحذف حتى يمكن إنشاء خطة جديدة لنفس الفصل لاحقًا بدل محاولة تحديث سجل محذوف.
- إضافة رسائل تشخيص واضحة لأخطاء 401/403/404/400/422/5xx.
- تحديث Cache Version إلى `school-1.0.62`.

