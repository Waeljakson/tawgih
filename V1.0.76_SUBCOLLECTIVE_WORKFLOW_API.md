# V1.0.76 — API جلسات متابعة الإرشاد الجمعي

تم تحويل عمليات الكتابة الخاصة بـ `Guidance_SubCollective` من Data API إلى Backend Workflow API المصدق بالمستخدم الحالي:

- `guidance_save_subcollective` للحفظ.
- `guidance_update_subcollective` للتعديل.
- `guidance_delete_subcollective` للحذف.

## التعديل بدون ID
يتم تحديد السجل الأصلي بواسطة: العام الدراسي، المجمع، المرحلة، الفصل الدراسي، تاريخ الجلسة الأصلي، اسم الجلسة الأصلي، و`Target` الأصلي.

## الربط
يتم توليد `Target` تلقائيًا للحفاظ على علاقة الجلسة التابعة بالجلسة الرئيسية وتسلسل الثانية/الثالثة/الرابعة. لا يطلب من المستخدم إدخال أي معرف يدوي.

## الواجهة
زر إضافة جلسة يفتح المتابعة التالية من موعد الجلسة القادمة. بعد الحفظ أو حفظ التعديل يتم فتح تقرير الإرشاد الجمعي مباشرة.
