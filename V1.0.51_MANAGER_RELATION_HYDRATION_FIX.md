# V1.0.51 — School manager relation hydration fix

- Fixes automatic School Manager display in Distribution Data when Bubble returns relation fields as ID-only objects.
- Hydrates `Users Data -> Current Job` against `job_titels` before reading `Job Titel`.
- Hydrates employee School relations against the School directory before matching the current user's assigned School.
- Hydrates direct School -> manager/principal relation against Users Data when present.
- Keeps manager selection restricted to the current School and excludes complex/campus manager roles.
- Cache version bumped to `school-1.0.51`.
