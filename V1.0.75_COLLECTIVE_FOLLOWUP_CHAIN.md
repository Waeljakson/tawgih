# V1.0.75 — سلسلة جلسات متابعة الإرشاد الجمعي

تم توظيف جدول Bubble الحالي `Guidance_SubCollective` لجلسات المتابعة التابعة لجلسة الإرشاد الجمعي، دون أي تعديل على بنية قاعدة البيانات أو حذف للسجلات القديمة.

## السلوك
- الجلسة الرئيسية تبقى في `Guidance_Collective` وبها أسماء المشاركين.
- يمكن إضافة 3 جلسات متابعة كحد أقصى، فتكون السلسلة: الرئيسية، الثانية، الثالثة، الرابعة.
- زر `+ إضافة جلسة` يظهر بجوار `تعديل` في آخر جلسة محفوظة فقط، بشرط وجود `موعد الجلسة القادمة`.
- عنوان جلسة المتابعة ينسخ تلقائيًا من الجلسة السابقة ويكون للقراءة فقط.
- تاريخ جلسة المتابعة يساوي `موعد الجلسة القادمة` في الجلسة السابقة ويكون للقراءة فقط.
- نموذج المتابعة يحتوي نفس مكونات الجلسة الأصلية العملية، لكن بدون إعادة أسماء المشاركين.
- بعد حفظ المتابعة يعود المستخدم إلى تقرير الإرشاد الجمعي.

## الربط دون تغيير Schema
يستخدم الحقل النصي الحالي `Target` في `Guidance_SubCollective` كوسم ارتباط مقروء يحتوي على نوع السجل، رقم الجلسة، تاريخ الجلسة الرئيسية والصف عند توفره. لا تتم إضافة أي Field جديد.

## التقرير
- التقرير يحتفظ بالأعمدة الثلاثة: اسم الجلسة، المشاركون، الزمن.
- الجلسة الرئيسية تحمل شارة واضحة.
- جلسات المتابعة تظهر أسفلها بتدرج بصري وشارات: الجلسة الثانية / الثالثة / الرابعة.
- خانة المشاركين في جلسات المتابعة تعرض `نفس المشاركين في الجلسة الرئيسية` بدل تكرار الأسماء.

## التوافق
- سجلات `Guidance_SubCollective` القديمة غير التابعة للإرشاد الجمعي لا تدخل في هذه السلسلة؛ يتم التعرف على جلسات المتابعة فقط بواسطة وسم واضح خاص بها في `Target`.
