# V1.0.71 — Guidance_Time from guidance_bootstrap

- Reads `guidance_times` returned by `guidance_bootstrap`.
- Supports Bubble Option Set lists serialized as plain Display strings.
- Populates the collective-guidance duration dropdown from `Guidance_Time`.
- Sends the selected Display value to save/update workflows.
- Removes the incorrect Data API lookup attempt for `Guidance_Time` because it is an Option Set.
- No Bubble database schema changes.
