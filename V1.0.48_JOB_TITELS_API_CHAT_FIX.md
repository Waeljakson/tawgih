# V1.0.49

Live API confirmation:
- endpoint: `job_titels`
- HTTP 200
- Bubble display data type: `Job Titels`
- label field: `Job Titel`
- Users Data relation: `Current Job`

The shared directory now loads `job_titels` as the authoritative source,
resolves Current Job IDs to `Job Titel`, and the chat then filters for
`موجه طلابي` while displaying `Users Data -> Full Name`.
